# Build outputs
/dw-browser
*.exe
*.test
*.out
bin/

# Environment & secrets
.env
.env.*
*.key
*.pem
credentials.*

# Private docs (real content in deepwork-pro; symlinked here, not committed).
# 用 `docs`(不带斜杠)—— docs 是符号链接,`docs/` 只匹配目录、抓不到符号链接。
docs
evolve_history

# Editor
.DS_Store
.idea/
.vscode/
